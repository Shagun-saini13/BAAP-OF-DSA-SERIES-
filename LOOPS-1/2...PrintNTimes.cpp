//doubt
#include<iostream>
using namespace std;
int main(){
    int n;
    cout<<"Enter n: ";
    cin>>n;

    for(int i=1 ; i<=n ; i++){
        cout<<"Alpha Intern"<<endl;
        return 0;// by using return 0 if n=2 , alpha intern print only one time
    }
}